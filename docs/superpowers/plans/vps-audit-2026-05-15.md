# VPS Audit — 2026-05-15

**VPS:** root@76.13.8.113 (srv1282987)
**Path:** `/root/webtop/`
**Method:** SSH key-based (no password prompt)
**Backup:** `/root/backups/webtop_audit_2026-05-15.tar.gz` (62 KB)

---

## תמונת מצב מערכת

### PM2 — 2 תהליכים
- **webtop** — pid 29562, uptime 72m, **18 restarts** ← רב מדי, חשד לזנבות
- **elal-scraper** — לא קשור לפרויקט הזה, מתעלם

**לוגים אחרונים:** push מגיע כל 15 דק׳ עבור `userId=a1057d56-9d85-4564-8e3d-88b3642c4c96` (אלדד גונשרוביץ — parent), 35-36 notifications כל פעם. רגוע. אין שגיאות חוץ מ-`409 Conflict` ב-Telegram getUpdates (מעיד על שני polls שרצים — לבדוק).

### `.env` ב-VPS — מה מוגדר

| Key | Value (משוערך/מצונזר) |
|---|---|
| `PORT` | `3001` |
| `VPS_URL` | `http://76.13.8.113:3001/api/push` |
| `USE_LOCAL_SCRAPER` | `false` ← השרת לא scrape, רק מקבל push מהבית. ✓ |
| `TELEGRAM_CHAT_ID` | `7773889743` (אלדד) |
| `TELEGRAM_EXTRA_CHAT_IDS` | `6642684065` (**מורן! כבר שם** ✓✓) |
| `TELEGRAM_BOT_TOKEN` | `<REDACTED>` |
| `JWT_SECRET` | `<REDACTED>` — קיים ✓ |
| `TOKEN_ENC_KEY` | `<REDACTED>` — קיים ✓ |
| `PUSH_SECRET` | `<REDACTED>` |
| `PUSH_DEFAULT_USER_ID` | `a1057d56-9d85-4564-8e3d-88b3642c4c96` (אלדד גונשרוביץ — parent) |
| `ENABLE_ADMIN_UI` | `true` ← אדמין UI **פעיל** ב-prod |
| `ADMIN_UI_PASSWORD` | `<REDACTED>` |
| `WEBTOP_HEADLESS`, `WEBTOP_PASS`, `WEBTOP_SESSION`, `WEBTOP_USER` | קיימים — לא רלוונטי כי `USE_LOCAL_SCRAPER=false` |

**חסר במכוון:** `ENABLE_PUBLIC_REGISTRATION` — לכן `/register` נותן 404 כברירת מחדל. טוב לעכשיו.

**משמעות:**
- מורן כבר ב-`TELEGRAM_EXTRA_CHAT_IDS`. אין צורך בשינוי כדי שתקבל התראות.
- `JWT_SECRET` ו-`TOKEN_ENC_KEY` קיימים. נוכל לאכוף auth ב-Phase 2 ללא יצירת keys חדשים.
- `PUSH_DEFAULT_USER_ID` מצביע על **a1057d56** (parent, "אלדד גונשרוביץ"), לא על המנהל. כל ה-cache והdata נטענים תחת ה-userId הזה. זה ה-cache שאלדד רואה כשהוא נכנס היום.

### `users.json` ב-VPS

יש **שני משתמשים**:

1. **97116e3a-8365-41bf-92b1-9cf022a111e5**
   - שם: "מנהל"
   - טלפון: `054-4956647`
   - chatId: `7773889743` (אלדד)
   - role: `admin`
   - status: `active`
   - loginCount: 4, lastLogin: 2026-04-09
   - lastLoginIp: `80.230.176.214, 172.19.0.7` ← Traefik proxy
   - passwordHash: קיים
   - webTokenEncrypted: `null` ← אין token
   - children: `[]` ← ריק!

2. **a1057d56-9d85-4564-8e3d-88b3642c4c96**
   - שם: "אלדד גונשרוביץ"
   - טלפון: `0546778353` (טלפון שונה!)
   - chatId: `null`
   - role: `parent`
   - status: `active`
   - loginCount: 1, lastLogin: 2026-03-24
   - lastLoginIp: `::1` (localhost — נוצר במחשב הבית?)
   - passwordHash: קיים (שונה מהראשון)
   - **webTokenEncrypted**: לא מוצג בקטע שקראתי — לבדוק אם יש לו token
   - children: `[]`

**אבחנה חשובה:** אלדד גונשרוביץ עם הטלפון `0546778353` הוא בעל ה-data — push_loop בבית דוחף נתונים עבור user זה (לפי הלוג). ה-"מנהל" עם טלפון `054-4956647` הוא admin אבל **לא** הילדים מקושרים אליו.

**הילדים** מקושרים דרך `children_config.json` (גלובלי) ולא דרך `users.json`. ה-`/api/data` מחזיר את הילדים של ה-`PUSH_DEFAULT_USER_ID`.

### קבצי data ב-VPS

- `data_cache.json` (44 KB) — קובץ legacy של בעלי גירסה ישנה
- `data_cache_97116e3a-8365-41bf-92b1-9cf022a111e5.json` (48 KB) — cache של admin, מ-Mar 4 (ישן)
- `data_cache_a1057d56-9d85-4564-8e3d-88b3642c4c96.json` (70 KB) — cache של "אלדד גונשרוביץ" parent, **מתעדכן בזמן אמת** (פעיל)
- `children_config.json` (45 KB) — כולל תמונות
- `homework_status.json` (58 KB)

### סקריפטים ידניים שנוצרו ב-VPS (לא ב-git)

1. **`activate_pending_user.cjs`** — סקריפט לקריאה ידנית `--list` או `<phone>` כדי להפוך pending → active. שימוש בעבר כדי לעקוף את ה-admin UI.
2. **`dedupe_users_by_phone.cjs`** — מנקה כפילויות.

הסקריפטים הללו מעידים שמישהו עבד ידנית ב-VPS לתקן בעיות. **חשוב**: לא להפעיל אותם בעיוורון.

### `.env.bak-admin-` קיים

יש backup של ה-`.env` עם סופת `admin-` — כנראה לפני שינוי הקשור ל-admin password. לא נוגעים.

---

## ממצאים קריטיים

1. ✓ **מורן כבר מקבלת התראות** — `TELEGRAM_EXTRA_CHAT_IDS=6642684065`. אין מה לעשות בשלב 1B!
2. ✓ **JWT_SECRET ו-TOKEN_ENC_KEY כבר ב-`.env`** — מוכן לשלב 2A.
3. ⚠️ **18 PM2 restarts** — לא נחקר. אולי קריסות. אולי `pm2 reload`. לבדוק `pm2 logs` עם `--err` לסשן הבא.
4. ⚠️ **409 Conflict ב-Telegram** — שני polling שרצים? אולי `pm2 restart webtop` יצר זה זמני, אבל אם זה ממשיך נצטרך לוודא רק instance אחד.
5. ⚠️ **`PUSH_DEFAULT_USER_ID` הוא parent ולא admin** — זה אומר שכשאלדד נכנס לאפליקציה (בלי JWT), הוא בעצם רואה את ה-cache של ה-parent. מבחינת UX זה עובד היום, אבל ב-Phase 2 כשנכפה auth, אלדד יצטרך להתחבר כ-admin, ואז ה-`getAuthenticatedData(req)` יחזיר את ה-cache של `req.user.id` = ה-admin (`97116e3a...`) שהcache שלו ישן/ריק. **תיקון לוגי נדרש**: כשמשתמש מחובר הוא admin, להמשיך להחזיר את ה-cache של `PUSH_DEFAULT_USER_ID` (זה הילדים שלו).
6. ⚠️ **שני משתמשים שניהם נקראים אלדד** — מבלבל. ה-admin "מנהל" (054-4956647) וה-parent "אלדד גונשרוביץ" (0546778353). חייבים להבין אם אלה שני טלפונים שונים של אותו אדם, או שיש חוסר עקביות.

---

## פעולות נדרשות מהאודיט

- [ ] לאשר עם המשתמש: שני המשתמשים → האם זה אותו אדם עם 2 טלפונים? או שצריך לאחד?
- [ ] לבדוק `pm2 logs webtop --err --lines 100 --nostream` לסשן הבא (לראות sources לcrashes)
- [ ] לבדוק אם `409 Conflict` חוזר אחרי `pm2 restart webtop` יחיד
- [ ] שלב 1B: **בוטל** — מורן כבר ב-env. נמחק מ-todos.
- [ ] שלב 2 — להוסיף ל-`getAuthenticatedData`: לבן admin → fallback ל-`PUSH_DEFAULT_USER_ID`
- [ ] להעתיק את הbackup מקומית: `scp root@76.13.8.113:/root/backups/webtop_audit_2026-05-15.tar.gz .`

---

## סיכום נטו

**המצב טוב יותר ממה שחשבנו:**
- מורן כבר מקבלת התראות
- כל ה-keys הקריפטוגרפיים קיימים
- ה-VPS יציב (uptime 72m), push זורם רגיל

**הסיכון העיקרי לשלב 2:**
- שני המשתמשים ב-users.json מבלבלים. צריך להחליט מי המייצג של אלדד.
- ההפרדה הלוגית בין admin (97116e3a) ל-data owner (a1057d56) דורשת תיקון ב-`getAuthenticatedData` לפני שאוכפים auth.

# Node
node_modules/
npm-debug.log*

# Environment & secrets
.env
*.env
.env.dev
.env.local

# DEV-only data files
users.dev.json
data_cache_dev_*.json

# Backups
backups/

# Playwright session (contains login cookies)
.webtop_session.json

# Browser profile (cookies, session — NEVER commit)
.webtop_profile/

# Runtime data
.telegram_session_last.txt
homework_status.json
data_cache.json
sent_reminders.json
push_scrape.log
push_loop_run.log
push_loop_run.err.log
.push_loop.lock
.scraping_lock
daemon.log
discovery.json

# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.venv

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.cursor/
scrape_output*.json
users.json
data_cache_*.json

# Codex / tooling logs
.codex-*.log
.codex-*.out.log
.codex-*.err.log
watchdog.log

# DEV cache + audit artifacts (local-only)
dev_data_cache_*.json
dev_sent_reminders_*.json
vps_data_check.json
audit_results.json
app_feed.png

# Playwright MCP scratch
.playwright-mcp/
